<h1>So many bands to choose from!</h1>

<ul>
  <% @bands.each do |band| %>
    <li><a href="<%= band_url(band)%>"><%= band.name</a></li>
  <% end %>
</ul>

<a href="<%= new_band_url %>">Create Band</a>